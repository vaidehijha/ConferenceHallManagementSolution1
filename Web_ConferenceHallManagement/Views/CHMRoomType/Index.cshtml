@model IEnumerable<MasterCHRoomTypeVM>

@{
    ViewData["Title"] = "Index";
}

<div class="container">


    <h3 class="text-decoration-underline"><i class="bi bi-list"></i>&nbsp;Master Conferenece Hall Room Types</h3>

    <p>
        <a class="btn btn-warning my-2" asp-action="Create"><i class="bi bi-plus-circle"></i>&nbsp;&nbsp;Create New</a>
    </p>
    <table id="statustable" class="table table-bordered table-hover display">
        <thead>
            <tr class="bg-secondary text-light">
                <th>
                    @Html.DisplayNameFor(model => model.RoomTypeId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.RoomTypeEn)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.RoomTypeHi)
                </th>

                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.RoomTypeId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.RoomTypeEn)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.RoomTypeHi)
                    </td>

                    <td>
                        <a class="btn btn-warning btn-sm" asp-action="Edit" asp-route-id="@item.RoomTypeId"><i class="bi bi-pencil-square"></i>&nbsp;Edit</a> |
                        <a class="btn btn-success btn-sm" asp-action="Details" asp-route-id="@item.RoomTypeId"><i class="bi bi-binoculars"></i>&nbsp;Details</a> |
                        <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.RoomTypeId"><i class="bi bi-x-circle"></i>&nbsp;Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<script>
    $(document).ready( function () {
        $('#statustable').DataTable();
    } );

</script>
