@model ConferenceHallSessionVM

@{
    ViewData["Title"] = "Details";
}

<div class="container">
    <h1>Details</h1>

    <div>
        <h4 class="text-decoration-underline">@(ViewBag?.HallName ?? "Hall") - Session</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.SessionEn)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.SessionEn)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.SessionHi)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.SessionHi)
            </dd>

        </dl>
    </div>
    <div>
        <a class="btn btn-warning" asp-action="Edit" asp-route-id="@Model?.SessionId"><i class="bi bi-pencil-square"></i>&nbsp;Edit</a> |
        <a class="btn btn-secondary" asp-action="Index" asp-route-id="@Model?.HallId"><i class="bi bi-list"></i>&nbsp;Back to List</a>
    </div>
</div>
